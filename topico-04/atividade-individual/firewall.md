# Firewall

O sistema encontrava-se inicialmente com o firewall UFW inativo.

Foi ativado e configurado apenas para permitir o tráfego HTTP:

- Porta 80/TCP (serviço web)

As portas 22 (SSH) e 443 (HTTPS) não foram configuradas, pois não estão em utilização.

